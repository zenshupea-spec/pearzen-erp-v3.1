'use client';

import { PortalSessionControls } from '../../../components/portal/PortalSessionProvider';

/** Idle countdown + staff profile for OM command shell top bar. */
export default function OmCommandPortalBar() {
  return <PortalSessionControls variant="inline" />;
}
