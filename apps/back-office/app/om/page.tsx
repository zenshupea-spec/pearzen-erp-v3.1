import OperationsPage from '../executive/operations/page';

/** OM portal home — same live radar as Executive CV Operations, in OM command shell. */
export default function OmPortalPage() {
  return <OperationsPage portal="om" />;
}
