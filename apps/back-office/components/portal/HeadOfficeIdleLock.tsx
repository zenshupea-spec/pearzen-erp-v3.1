'use client';

import PortalSessionProvider from './PortalSessionProvider';

/** @deprecated Use PortalSessionProvider directly. */
export default function HeadOfficeIdleLock() {
  return <PortalSessionProvider />;
}
